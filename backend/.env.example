# No secrets required for the default local/free-tier setup (SQLite + open-source HF models).
# If you swap in the Gemini API for anything, put the key here.
GEMINI_API_KEY=
DATABASE_URL=sqlite:///./shopping_assistant.db
CORS_ORIGIN=http://localhost:5173
